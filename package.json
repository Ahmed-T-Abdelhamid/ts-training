{
  "name": "ts",
  "version": "1.0.0",
  "description": "",
  "main": "app.ts",
  "scripts": {
    "test": "jest",
    "commit": "git add -A; && git commit -m \"done!\";",
    "revert": "git reset --hard",
    "tcr": "npm test && npm run commit || npm run revert"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.4.2",
    "typescript": "^4.0.2"
  }
}
